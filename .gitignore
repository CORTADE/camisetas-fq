node_modules/
dist/
.astro/
.env
.env.local
.DS_Store
.vscode/
.idea/
*.log

# Original reference HTML — visual source of truth, must NEVER be tracked or modified.
# All future work goes through src/ and public/, never into this file.
# Leading slash scopes this to the repo root only (so public/admin/index.html etc. are NOT ignored).
/index.html
.claude/
